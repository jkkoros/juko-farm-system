# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/farmdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Jkk20239747!.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Optional: Disable H2 console (since we're not using H2)
spring.h2.console.enabled=false

# Optional: Change server port (default is 8080, change if needed)
server.port=8085